syntax = "proto3";

package userocpc;

message SingleUser {
    string ideaid = 1;
    string userid = 2;
    string cost = 3; // total cost group by userid
    string ctrcnt = 4; // ctr count group by userid
    string cvrcnt = 5; // cvr count group by userid
    string adclass = 6;
    string adclass_cost = 7;
    string adclass_ctrcnt = 8;
    string adclass_cvrcnt = 9;
}


message UserOcpc {
    repeated SingleUser user = 1;
}